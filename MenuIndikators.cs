using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class MenuIndikators : MonoBehaviour
{
    public TMP_Text moneyinmenu;

    // Start is called before the first frame update
    void Start()
    {
        moneyinmenu.text = "";
    }

    // Update is called once per frame
    void Update()
    {
        moneyinmenu.text = "";
    }
}
